<%= form_for(@news_category) do |f| %>
  <% if @news_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@news_category.errors.count, "error") %> prohibited this news_category from being saved:</h2>

      <ul>
      <% @news_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :position %><br />
    <%= f.text_field :position %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
