<h1>Listing news_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Position</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @news_categories.each do |news_category| %>
  <tr>
    <td><%= news_category.name %></td>
    <td><%= news_category.position %></td>
    <td><%= link_to 'Show', news_category %></td>
    <td><%= link_to 'Edit', edit_news_category_path(news_category) %></td>
    <td><%= link_to 'Destroy', news_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New News category', new_news_category_path %>
